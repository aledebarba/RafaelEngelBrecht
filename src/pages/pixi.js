import React from 'react'
import styled from 'styled-components'

const Indexpixi = () => {
    return (
        <UseLocalStyles>
        </UseLocalStyles>
    )
}

export default Indexpixi

const UseLocalStyles = styled.div`
    background-color: #facada;
`